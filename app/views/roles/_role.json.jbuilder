json.extract! role, :id, :title, :checklist_user, :checklist_manager, :user_manager, :organization_manager, :audit_manager, :audit_user, :primary, :admin, :created_at, :updated_at
json.url role_url(role, format: :json)
